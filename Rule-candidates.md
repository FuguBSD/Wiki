# Rule candidates

Each item is a candidate rule that a campaign produced. An item that reached a
specification carries `Delivered:` with the unit it became.

- 2026-08-25 probe each declared offer before a campaign, because a policy
  document is not a permission. Delivered: FuguSTX TRN-INST
- 2026-08-26 an IAM rule holds permission sets of one scope type only, so a
  mixed policy needs two rules. Delivered: FuguSTX IAC-APPLY-6
- 2026-08-28 pass `default-project-id` at every key mint, because the default
  binds the key to the organization default project.
- 2026-08-28 pin an image tag only after a paginated registry probe.
- 2026-08-28 give every quiet remote step SSH keepalives, because a NAT drops a
  silent stream at ten minutes.
- 2026-08-29 probe each transport after a pin change, because one transport can
  hide a break in another.
- 2026-08-29 dispatch a teardown at the end of the work, because a cron schedule
  is best effort.

- 2026-08-31 back every schedule with a lease tag, because the GitHub `*/30`
  cron fires hours apart.
- 2026-08-31 budget near six minutes for a first serve on a fresh instance, not
  tens of minutes.
- 2026-08-31 retry an idempotent verb once on a runner-side TLS fault, because
  the fault sits outside the verb.
